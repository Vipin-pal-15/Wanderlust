# New Model : Reviews (IN models review.js)
.comment
.rating(1 to 5)
.createdAt 

In show page create review

# CREATE Reviews
submitting the form

POST /listing/:id/reviews

# Render Review

# Topics 
.Express Router
.Cookies

These are used for making too the code restructure and better manner

# What is state?
#Stateful Protocol=>Stateful protocol require server to save the status and session information.
eg-ftp[File transfer protocol]

#Stateless Protocol=>Stateless protocol does not require the server to retain the server information
eg-http[hyper text transfer protocol].

# connect-flash
Those type message jo appeare[dikhe] ho ek time aur page refresh krne pr hat jo to use connect-flash
kahte hai.

# res.locals

# Passport [helps us to the hash password]

npm i passport
npm i passport-local
npm i passport-local-mongoose

# Image Upload
when upload to the file then the mongodb has two draback
1)send file problem
2)size limit problem

enctype="mutipart/form-data"

# Cloud Setup=>
cloudinary & .env file
npm i cloudinary
npm i multer-storage-cloudinary

cloudConfig make new file in outer and use these file in 'listing.js' file in routes folder
after these process changes the schema in image
